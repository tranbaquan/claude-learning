# MCP Setup

## Playwright
> Note: Playwright MCP is an alternative way to use Playwright with Coding Agents CLI (Ex: Claude).
> It is not required if you are using Playwright CLI Skills and vice versa.

Use below command in other to add Playwright MCP to project:

```shell
claude mcp add --scope project playwright npx @playwright/mcp@latest
```